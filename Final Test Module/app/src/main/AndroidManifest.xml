<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mayan.finaltest" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
     To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" /> <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logotest"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TestPage"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".PreResult2"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".SignUp"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".Result"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".Questions"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".Result2"
            android:label="@string/app_name" >
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".TR"
            android:label="@string/title_activity_tr" >
        </activity>
        <activity
            android:name=".Resultl"
            android:label="@string/title_activity_resultl" >
        </activity>
        <activity
            android:name=".preres"
            android:label="@string/title_activity_preres" >
        </activity>
        <activity
            android:name=".Help"
            android:label="@string/title_activity_preres" >
        </activity>
    </application>

</manifest>
